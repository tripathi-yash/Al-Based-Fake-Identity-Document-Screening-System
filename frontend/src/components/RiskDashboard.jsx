import React from "react";

// TODO: display risk_band, evidence_band, final_recommendation, and
// contributing_flags from the /screen response, per schema/module_io_schema.json
// -> risk_scoring_engine. Color-code: clear = green, secondary_check = amber,
// high_risk = red. This is what judges will look at most - keep it clear.
export default function RiskDashboard() {
  return <div>{/* risk score display goes here */}</div>;
}
