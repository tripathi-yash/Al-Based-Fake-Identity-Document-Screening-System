import React, { useState } from "react";

// TODO: form fields - doc image, live selfie, doc_type dropdown
// (see action_plan.pdf Section 11 - officer selects doc type manually,
// no auto-classification). POST to /screen on submit.
export default function UploadForm() {
  return <div>{/* upload form goes here */}</div>;
}
